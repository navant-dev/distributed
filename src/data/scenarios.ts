export const scenarioLevels = ["Intermediate", "Senior", "Staff+"] as const;
export type ScenarioLevel = (typeof scenarioLevels)[number];

export type Scenario = {
  slug: string;
  title: string;
  summary: string;
  category: string;
  level: ScenarioLevel;
  role: string;
  duration: number;
  context: { business: string; architecture: string; invariant: string };
  signals: { time: string; observation: string }[];
  constraints: string[];
  prompts: { phase: string; question: string; nudge: string }[];
  response: { diagnosis: string; immediate: string[]; recovery: string[]; prevention: string[]; tradeoffs: string };
  rubric: { dimension: string; strongSignal: string; points: number }[];
  traps: string[];
  related: { label: string; href: string }[];
};

export const scenarios: Scenario[] = [
  {
    slug: "checkout-retry-storm",
    title: "Checkout is melting under a retry storm",
    summary: "A slow payment dependency turns ordinary retries into a cascading failure across checkout.",
    category: "Reliability", level: "Senior", role: "Incident commander", duration: 25,
    context: {
      business: "Checkout handles 18k requests per second during a flash sale. Abandoned carts are rising by 4,000 per minute.",
      architecture: "API gateway → checkout service → inventory reservation and payment authorization. Each layer retries twice with exponential backoff, but deadlines are not propagated.",
      invariant: "A customer must never be charged twice, and confirmed inventory must not be silently lost.",
    },
    signals: [
      { time: "12:00", observation: "Payment p99 rises from 380 ms to 4.8 s; error rate remains below 2%." },
      { time: "12:03", observation: "Checkout concurrency triples and outbound payment traffic reaches 2.7× incoming traffic." },
      { time: "12:06", observation: "Thread pools saturate; healthy inventory calls now time out behind payment work." },
      { time: "12:09", observation: "Payment recovers, but checkout remains overloaded and queues continue growing." },
    ],
    constraints: ["The payment provider cannot be changed during the incident.", "The business will accept degraded conversion before duplicate charges.", "Inventory reservations expire after 12 minutes."],
    prompts: [
      { phase: "Diagnose", question: "What evidence distinguishes dependency slowness from insufficient checkout capacity?", nudge: "Compare arrival rate, useful completions, retry amplification, queue time, and saturation." },
      { phase: "Stabilize", question: "Which controls do you apply first, and in what order?", nudge: "Protect the invariant and stop amplification before adding capacity." },
      { phase: "Recover", question: "How do you resolve timed-out payments and inventory reservations safely?", nudge: "A timeout is an unknown outcome, not a failed operation." },
      { phase: "Prevent", question: "What service contract and telemetry would keep this failure bounded next time?", nudge: "Think deadlines, budgets, admission control, operation identity, and recovery probes." },
    ],
    response: {
      diagnosis: "This is retry amplification plus queue collapse. A slow dependency increases in-flight work; layered retries multiply it; expired callers leave useless work consuming capacity. The fact that checkout stays unhealthy after payment recovers points to backlog and synchronized retries, not a permanent capacity shortage.",
      immediate: ["Freeze nonessential changes and declare duplicate charge prevention the protected invariant.", "Disable or sharply budget retries at one controlled layer; propagate remaining request deadlines.", "Cap payment concurrency and shed low-priority or already-expired checkout work.", "Open a circuit for new payment attempts while allowing sparse, jittered recovery probes."],
      recovery: ["Reconcile every ambiguous authorization by stable payment-attempt ID before retrying.", "Return recorded outcomes for duplicate idempotency keys instead of reapplying side effects.", "Drain or discard expired queue entries, then ramp admission gradually while watching useful throughput.", "Reconcile inventory holds against durable payment outcomes before releasing or confirming them."],
      prevention: ["One end-to-end retry budget, not independent retries at every hop.", "Persist idempotency key and payment outcome atomically with checkout state.", "Alert on retry amplification, queue age, deadline-exceeded work, and useful completion rate.", "Test slow dependencies and recovery waves with load, not only hard failures."],
      tradeoffs: "Aggressive shedding sacrifices short-term conversion but protects correctness and lets the system recover. More servers can temporarily absorb concurrency, but without bounded work they only postpone collapse and may amplify pressure on payment.",
    },
    rubric: [
      { dimension: "Failure model", strongSignal: "Names amplification, queueing, and ambiguous outcomes rather than calling this only a latency incident.", points: 3 },
      { dimension: "Safety", strongSignal: "Uses stable operation identity and reconciliation to prevent duplicate charging.", points: 3 },
      { dimension: "Control loop", strongSignal: "Bounds concurrency, retries, deadlines, admission, and recovery probes coherently.", points: 3 },
      { dimension: "Communication", strongSignal: "States the protected invariant, degradation policy, and measurable recovery gates.", points: 1 },
    ],
    traps: ["Blindly increasing retry count", "Treating every timeout as a failed payment", "Autoscaling checkout without limiting downstream concurrency", "Closing the incident as soon as payment latency recovers"],
    related: [{ label: "Failure and retry patterns", href: "/topics/reliability/failure-retry-patterns/" }, { label: "Circuit breakers", href: "/topics/reliability/circuit-breakers/" }, { label: "Load shedding", href: "/topics/reliability/load-shedding/" }],
  },
  {
    slug: "stale-reads-after-failover",
    title: "Users see stale balances after regional failover",
    summary: "Failover restores availability, but asynchronous replicas expose older account state.",
    category: "Consistency", level: "Senior", role: "Service owner", duration: 25,
    context: { business: "A wallet product fails traffic from us-east to us-west. Some users see a lower balance and repeat transfers.", architecture: "One write leader per account region, asynchronous cross-region replication, CDN-cached profile summaries, and clients that may retry against the new region.", invariant: "A confirmed transfer must not be applied twice, and available funds must never be overstated." },
    signals: [{ time: "09:14", observation: "East loses power after acknowledging transfers through log position 8,442,190." }, { time: "09:17", observation: "West is promoted at position 8,441,760; observed replication lag was reported as 0 seconds just before failure." }, { time: "09:20", observation: "Support reports balances moving backward after refresh." }, { time: "09:25", observation: "Old east instances intermittently accept health checks as networking returns." }],
    constraints: ["A global write outage longer than five minutes triggers regulatory escalation.", "The ledger is authoritative; cached balance summaries are derived.", "The old region may return with writes not present in west."],
    prompts: [{ phase: "Diagnose", question: "What does the log-position gap tell you that the lag-in-seconds metric did not?", nudge: "Freshness is about replicated work and acknowledgements, not wall-clock delay alone." }, { phase: "Stabilize", question: "How do you prevent two writable primaries while preserving a safe customer experience?", nudge: "Promotion needs authority, fencing, and an explicit degraded mode." }, { phase: "Recover", question: "How do you reconcile acknowledged-but-missing transfers?", nudge: "Separate the immutable ledger from derived balances and client retries." }, { phase: "Prevent", question: "What failover contract would make RPO visible to the product?", nudge: "Define readiness using positions, fencing, and business invariants." }],
    response: { diagnosis: "The promoted replica is behind despite a misleading time-lag metric. Reads regress, retries can duplicate acknowledged transfers, and the returning region creates split-brain risk unless its old lease or epoch is fenced.", immediate: ["Fence the old leader using a higher epoch at the write authority and downstream stores.", "Disable or shorten derived caches and mark balances as temporarily reconciling.", "Route transfer retries through idempotent operation IDs; reject writes whose authority epoch is stale.", "If available funds cannot be proven, fail closed for debits while allowing safe reads or deposits according to policy."], recovery: ["Compare durable ledger positions and recover missing acknowledged records from the old region or transaction journal.", "Deduplicate by transfer ID, replay into the authoritative ledger, then rebuild balance projections.", "Audit every response acknowledged beyond the promoted position and communicate affected accounts.", "Return east only as a follower after state convergence and epoch validation."], prevention: ["Promotion gates based on log sequence and acknowledged-write watermarks, not lag seconds.", "Consensus-backed or otherwise strongly fenced regional authority.", "Read-your-writes tokens or session watermarks for post-transfer reads.", "Document RPO behavior and rehearse failover with acknowledged-write gaps."], tradeoffs: "Failing closed for uncertain debits reduces availability but protects funds. Waiting for zero lag can violate recovery targets; the product must choose an explicit bounded-loss or synchronous-replication contract." },
    rubric: [{ dimension: "Authority", strongSignal: "Requires epoch-based fencing before promotion or rejoin.", points: 3 }, { dimension: "Data recovery", strongSignal: "Reconciles immutable operations before rebuilding derived balances.", points: 3 }, { dimension: "Client semantics", strongSignal: "Handles retries and monotonic/read-your-writes expectations.", points: 2 }, { dimension: "Product contract", strongSignal: "Makes RPO, degraded operations, and customer communication explicit.", points: 2 }],
    traps: ["Trusting lag measured only in seconds", "Clearing caches and assuming the data is repaired", "Promoting without fencing the previous leader", "Replaying transactions without stable IDs"],
    related: [{ label: "Geo-replication", href: "/topics/architecture/geo-replication/" }, { label: "RPO and RTO", href: "/topics/architecture/rpo-rto/" }, { label: "Consistency models", href: "/topics/consistency/consistency-models/" }],
  },
  {
    slug: "raft-election-instability",
    title: "A Raft cluster keeps changing leaders",
    summary: "Uneven latency and pauses create election churn without an obvious node failure.",
    category: "Consensus", level: "Intermediate", role: "On-call engineer", duration: 20,
    context: { business: "A configuration service backs service discovery for 600 applications. Writes stall every few minutes.", architecture: "Five Raft voters across three availability zones; two nodes share a noisy host class. Election timeout is 500–800 ms and heartbeats are 100 ms.", invariant: "Only committed configuration may be served, and two leaders must never commit conflicting entries in one term." },
    signals: [{ time: "14:02", observation: "Leader changes from n2 to n4; no process restart is recorded." }, { time: "14:04", observation: "n2 reports a 1.1 s runtime pause and n3 reports 900 ms disk fsync." }, { time: "14:05", observation: "Terms increase twice; commit index advances slowly but never diverges." }, { time: "14:09", observation: "Network median is 8 ms, while cross-zone p99 briefly exceeds 700 ms." }],
    constraints: ["No voter may be removed until quorum risk is evaluated.", "Configuration reads can tolerate brief staleness but not fabricated state.", "A rolling restart is possible only after the cluster stabilizes."],
    prompts: [{ phase: "Diagnose", question: "Why can healthy processes still cause elections?", nudge: "Raft reacts to missing timely communication, not the root cause of delay." }, { phase: "Stabilize", question: "What changes reduce churn without masking a genuinely dead leader?", nudge: "Compare observed tail latency and pauses with the election window." }, { phase: "Recover", question: "How do you verify safety while leadership changes?", nudge: "Terms, commit index, quorum membership, and log matching are your evidence." }, { phase: "Prevent", question: "Which SLOs and placement rules belong around the consensus group?", nudge: "Consensus depends on disks, runtime pauses, networks, and failure independence." }],
    response: { diagnosis: "Heartbeat processing is delayed beyond the election timeout by runtime pauses, fsync stalls, and cross-zone tail latency. Followers begin elections even though processes are alive. Raft preserves safety, but churn destroys write availability.", immediate: ["Stop membership changes and confirm a majority shares one committed prefix.", "Move or isolate the noisy workloads if reversible; reduce load on the control plane.", "Increase randomized election timeouts above measured worst-case healthy pauses while keeping heartbeat separation.", "Prefer the most up-to-date, stable candidate and monitor term rate and commit progress."], recovery: ["Verify term, leader, commit index, last-applied index, and log match across every voter.", "Catch lagging followers up before restarting or changing membership.", "Roll remediation one voter at a time, preserving a known healthy majority.", "Confirm client writes use leader redirection and bounded retry behavior."], prevention: ["Alert on elections per hour, fsync p99, runtime pauses, heartbeat delay, and commit latency.", "Use failure-independent placement and dedicated resource budgets for voters.", "Benchmark timeout values under realistic pauses rather than median network latency.", "Use pre-vote/check-quorum features where supported and tested."], tradeoffs: "Longer timeouts reduce false elections but lengthen detection of a real leader failure. The right value follows tail behavior and recovery objectives; it is not a universal constant." },
    rubric: [{ dimension: "Protocol reasoning", strongSignal: "Distinguishes safety from availability during election churn.", points: 3 }, { dimension: "Evidence", strongSignal: "Checks terms, quorum, commit index, and log matching before intervention.", points: 3 }, { dimension: "Mitigation", strongSignal: "Tunes against tail pauses and preserves quorum during rollout.", points: 2 }, { dimension: "Operations", strongSignal: "Connects consensus health to disk, runtime, network, and placement telemetry.", points: 2 }],
    traps: ["Restarting all voters together", "Assuming low median latency means the network is healthy", "Removing a slow voter before calculating quorum", "Treating leader churn as evidence of conflicting committed logs"],
    related: [{ label: "Raft", href: "/topics/consensus/raft/" }, { label: "Leader election", href: "/topics/consensus/leader-election/" }, { label: "Quorums", href: "/topics/consensus/quorums/" }],
  },
  {
    slug: "celebrity-hot-partition",
    title: "One celebrity account overwhelms a shard",
    summary: "A technically balanced keyspace collapses when one key becomes globally hot.",
    category: "Partitioning", level: "Intermediate", role: "Platform engineer", duration: 20,
    context: { business: "A social feed normally serves 1.2M reads per second. One livestream drives 180k requests per second to a single creator.", architecture: "Posts and counters are partitioned by creator ID using consistent hashing. Each partition has one write leader and two read replicas.", invariant: "Published posts must remain ordered for the creator; view counts may be approximate for up to five minutes." },
    signals: [{ time: "18:00", observation: "Fleet CPU remains 42%, but shard 317 reaches 100% CPU and 40× normal queue depth." }, { time: "18:01", observation: "Adding nodes moves ordinary ranges but creator 8842 remains on one partition." }, { time: "18:03", observation: "Replica reads help posts, while counter writes still saturate the leader." }, { time: "18:05", observation: "Cache hit rate drops as clients request many slightly different feed windows." }],
    constraints: ["Per-creator post order is externally visible.", "Clients cannot be updated during the event.", "Approximate counters are acceptable; missing posts are not."],
    prompts: [{ phase: "Diagnose", question: "Why did rebalancing fail despite spare fleet capacity?", nudge: "A hash function spreads many keys; it cannot divide one indivisible key." }, { phase: "Stabilize", question: "How do you split reads and writes according to their semantics?", nudge: "The post log and engagement counters do not require the same consistency." }, { phase: "Recover", question: "How do you migrate the hot key without breaking order?", nudge: "Use explicit routing state, epochs, and a cutover watermark." }, { phase: "Prevent", question: "What hot-key mechanism should exist before the next event?", nudge: "Detection and a reversible exception path matter more than perfect prediction." }],
    response: { diagnosis: "The distribution is balanced by key count but not by load. One creator key is indivisible under the current scheme, so ordinary ring rebalancing cannot spread its work.", immediate: ["Cache canonical post pages and coalesce identical misses.", "Fan read traffic across replicas while preserving a clear freshness contract.", "Buffer and aggregate commutative counter updates, shedding only nonessential precision.", "Apply per-tenant admission limits so one creator cannot consume recovery capacity."], recovery: ["Introduce a routing exception for the hot creator rather than reshuffling the entire ring.", "Keep ordered posts on one sequencer or partition substreams with an explicit merge sequence.", "Cut over using an ownership epoch and watermark; reject writers using stale routing.", "Drain aggregated counter deltas idempotently after the event."], prevention: ["Detect load by key, not only by node or partition.", "Support hot-key replication, request coalescing, and exception routing as tested control-plane operations.", "Separate workloads by invariant: ordered posts, cacheable reads, and commutative counters.", "Capacity-plan for concentration and enforce tenant fairness."], tradeoffs: "Replicating reads improves capacity but adds staleness. Salting writes spreads load but destroys simple per-key ordering and range access, so it should be limited to data whose merge semantics are explicit." },
    rubric: [{ dimension: "Diagnosis", strongSignal: "Identifies a single-key problem rather than generic imbalance.", points: 2 }, { dimension: "Semantic split", strongSignal: "Treats ordered posts, reads, and approximate counters differently.", points: 3 }, { dimension: "Migration", strongSignal: "Uses epochs and a watermark for safe routing cutover.", points: 3 }, { dimension: "Fairness", strongSignal: "Protects the rest of the fleet with admission control.", points: 2 }],
    traps: ["Adding nodes and expecting the hot key to split", "Randomly salting all creator data", "Serving unlimited stale cache without a contract", "Migrating ownership without fencing old writers"],
    related: [{ label: "Hot partitions", href: "/topics/partitioning/hot-partitions/" }, { label: "Consistent hashing", href: "/topics/partitioning/consistent-hashing/" }, { label: "Rebalancing", href: "/topics/partitioning/rebalancing/" }],
  },
  {
    slug: "duplicate-order-events",
    title: "Orders are fulfilled twice after a consumer restart",
    summary: "At-least-once delivery meets a non-idempotent warehouse side effect.",
    category: "Messaging", level: "Senior", role: "Application architect", duration: 25,
    context: { business: "A warehouse ships duplicate packages after a consumer deployment. Refund and recall costs are rising.", architecture: "Order events are read from a partitioned log. The consumer calls a warehouse API, writes a local processed-event row, then commits its offset.", invariant: "Each order may create at most one shipment, while no paid order may be silently skipped." },
    signals: [{ time: "03:10", observation: "Consumers restart after processing events but before committing offsets." }, { time: "03:11", observation: "The same event IDs are delivered again, as designed." }, { time: "03:12", observation: "The local deduplication row exists for some duplicates, but warehouse shipment IDs differ." }, { time: "03:20", observation: "Operators propose committing offsets before calling the warehouse." }],
    constraints: ["The log and local database support transactions independently, not together.", "The warehouse API accepts an optional client reference but does not document retention.", "Losing a paid order is worse than delayed fulfillment."],
    prompts: [{ phase: "Diagnose", question: "Where is the unsafe gap in the current operation order?", nudge: "List every crash point between external effect, local record, and offset commit." }, { phase: "Stabilize", question: "How do you stop new duplicates without losing events?", nudge: "Keep at-least-once input and make the side effect safely repeatable." }, { phase: "Recover", question: "How do you identify and reconcile already duplicated shipments?", nudge: "Event IDs alone are not enough if the external provider created new identities." }, { phase: "Prevent", question: "Which patterns work when the transactional boundary ends at an external API?", nudge: "Think idempotency contracts, inbox/outbox records, and explicit state machines." }],
    response: { diagnosis: "The warehouse call occurs outside the atomic boundary. A crash after shipment creation but before the dedup row or offset commit causes redelivery and a second external side effect.", immediate: ["Pause the affected consumer partitions while retaining the log backlog.", "Require a stable shipment idempotency reference derived from order ID for every warehouse call.", "Query the warehouse by that reference before retrying ambiguous requests.", "Do not commit offsets early; that trades duplicates for silent loss."], recovery: ["Join event IDs, orders, client references, and warehouse shipment records to locate duplicates.", "Define compensating actions—cancel, intercept, or refund—based on shipment state.", "Resume from known offsets with idempotent calls and bounded concurrency.", "Record every ambiguous external outcome for operator reconciliation."], prevention: ["Persist an inbox/state-machine transition before dispatch and an outbox command for shipment work.", "Contractually define idempotency-key scope and retention with the warehouse.", "Make consumers safe under crash injection at every boundary.", "Monitor duplicate deliveries separately from duplicate business outcomes."], tradeoffs: "At-least-once delivery is appropriate when loss is unacceptable, but exactly-once business outcomes require idempotency across the external boundary. A local transaction cannot make an undocumented remote API atomic." },
    rubric: [{ dimension: "Crash analysis", strongSignal: "Enumerates the side-effect/record/offset gaps precisely.", points: 3 }, { dimension: "External idempotency", strongSignal: "Uses a stable business key and verifies provider retention semantics.", points: 3 }, { dimension: "Recovery", strongSignal: "Reconciles existing shipments and defines compensation by state.", points: 2 }, { dimension: "Delivery semantics", strongSignal: "Rejects early offset commit as a data-loss trade.", points: 2 }],
    traps: ["Committing the offset before the side effect", "Assuming the broker can deduplicate warehouse calls", "Keeping deduplication records for less time than redelivery is possible", "Calling compensation without checking shipment state"],
    related: [{ label: "Delivery semantics", href: "/topics/messaging/delivery-semantics/" }, { label: "Idempotent workflows", href: "/topics/transactions/idempotent-workflows/" }, { label: "Distributed logs", href: "/topics/messaging/distributed-logs/" }],
  },
  {
    slug: "multi-region-inventory-split-brain",
    title: "Two regions sell the last unit",
    summary: "Active-active inventory remains available through a partition and violates uniqueness.",
    category: "Architecture", level: "Staff+", role: "Principal engineer", duration: 35,
    context: { business: "A limited-edition sale has one globally shared unit remaining. Both regions confirm an order during a WAN partition.", architecture: "Active-active APIs write to local databases. Inventory counters replicate asynchronously and conflicts resolve by last-write-wins timestamp.", invariant: "Confirmed sales must never exceed owned inventory for scarce items." },
    signals: [{ time: "10:00:00", observation: "Both regions read available=1 at local revision 912." }, { time: "10:00:02", observation: "Region A confirms order A-17; region B confirms B-42." }, { time: "10:00:05", observation: "WAN health shows 60% packet loss; local health remains green." }, { time: "10:03:00", observation: "Replication resumes and last-write-wins leaves available=0, hiding the double sale." }],
    constraints: ["Most catalog items should remain locally writable.", "Scarce releases may tolerate queuing or a brief rejection.", "Wall clocks are synchronized only within tens of milliseconds."],
    prompts: [{ phase: "Diagnose", question: "Why does last-write-wins hide rather than resolve this conflict?", nudge: "A converged value does not prove the invariant held across operations." }, { phase: "Stabilize", question: "What do you do with new orders and the two confirmations?", nudge: "Protect the invariant, preserve evidence, and define customer remediation." }, { phase: "Recover", question: "How do you determine the authoritative winner without relying on clocks?", nudge: "Authority and a serial order must come from the protocol or preallocated rights." }, { phase: "Prevent", question: "How would you preserve local writes for ordinary items but coordinate scarce inventory?", nudge: "Consistency can be selected per invariant and key." }],
    response: { diagnosis: "Two local read-modify-write operations consumed the same logical unit. Last-write-wins makes replicas converge but cannot restore the violated conservation invariant; timestamps do not create exclusive authority.", immediate: ["Stop or queue scarce-item sales in both regions and preserve both order histories.", "Choose an authoritative order using a predeclared sequencer or durable reservation order—not ad hoc wall time.", "Honor or compensate customers under an explicit business policy.", "Audit similar SKUs because the merged counter value conceals oversells."], recovery: ["Reconstruct operations from immutable order and reservation records.", "Recompute inventory from grants and consumptions rather than trusting the converged counter.", "Issue cancellation/compensation through idempotent workflows.", "Resume only after one authority or safe regional rights are established."], prevention: ["Route scarce-key reservations through a consensus-backed home region or global sequencer.", "Alternatively escrow inventory rights to regions so local decrements cannot exceed regional grants.", "Fence authority with epochs and reject stale writers.", "Use invariant-oriented reconciliation that detects conservation violations, not only replica divergence."], tradeoffs: "Global coordination adds latency and can reject work during partitions. Escrow keeps local availability but requires forecasting and rights transfers. The product can reserve stronger coordination for scarce keys while ordinary inventory remains active-active." },
    rubric: [{ dimension: "Invariant", strongSignal: "States conservation explicitly and explains why convergence is insufficient.", points: 3 }, { dimension: "Authority", strongSignal: "Uses consensus/sequencing or escrow rather than timestamps.", points: 3 }, { dimension: "Recovery", strongSignal: "Recomputes from immutable operations and handles customer remediation.", points: 2 }, { dimension: "Scope", strongSignal: "Applies coordination selectively by SKU risk.", points: 2 }],
    traps: ["Increasing clock synchronization accuracy", "Declaring the later timestamp the morally correct order", "Checking only that replicas eventually match", "Making the entire catalog globally serializable without product analysis"],
    related: [{ label: "Active-active", href: "/topics/architecture/active-active/" }, { label: "Conflict resolution", href: "/topics/architecture/conflict-resolution/" }, { label: "PACELC", href: "/topics/consistency/pacelc/" }],
  },
  {
    slug: "lease-clock-skew",
    title: "A clock jump creates two lease holders",
    summary: "Time-based ownership outlives its safety assumptions after virtualization pauses and clock correction.",
    category: "Coordination", level: "Staff+", role: "Distributed systems engineer", duration: 30,
    context: { business: "Two workers concurrently execute a daily settlement job, producing conflicting ledger exports.", architecture: "Workers acquire a database lease with an expiry timestamp based on local wall clocks. The resource accepts writes from any worker presenting the lease ID.", invariant: "At most one settlement worker may mutate a settlement generation." },
    signals: [{ time: "01:59:58", observation: "Worker A renews its lease through 02:00:28, then pauses for 38 seconds during host migration." }, { time: "02:00:30", observation: "Worker B observes the lease expired and acquires a new lease." }, { time: "02:00:36", observation: "Worker A resumes with its old lease and continues writing." }, { time: "02:00:40", observation: "NTP steps A's wall clock backward by 420 ms; both workers report valid ownership." }],
    constraints: ["The job may pause, but overlapping mutations are unacceptable.", "Workers and the database are in different failure domains.", "Some settlement steps call external systems."],
    prompts: [{ phase: "Diagnose", question: "Why can neither worker's local clock prove exclusive ownership?", nudge: "A paused process cannot observe that its authority expired elsewhere." }, { phase: "Stabilize", question: "How do you stop stale workers from mutating state?", nudge: "The protected resource must participate in rejecting old authority." }, { phase: "Recover", question: "How do you repair work when external steps may already have run twice?", nudge: "Fence internal writes and reconcile external operations by stable generation IDs." }, { phase: "Prevent", question: "What lease protocol and test cases make the timing assumptions explicit?", nudge: "Use monotonically increasing authority, bounded time assumptions, and pause testing." }],
    response: { diagnosis: "Lease expiry is observed independently, while the old holder can pause past expiry and resume. Wall-clock correction worsens reasoning, but the fundamental issue is that the resource accepts stale authority.", immediate: ["Pause settlement dispatch and fence both workers at the mutation store.", "Issue a new monotonically increasing fencing token to one elected worker.", "Require every protected write to carry the token and reject values below the highest seen.", "Record the affected settlement generation for reconciliation."], recovery: ["Compare writes by fencing token/generation and rebuild a canonical settlement output.", "Reconcile external calls using stable settlement and operation IDs; compensate duplicates by provider state.", "Do not simply delete one worker's records when downstream effects may exist.", "Resume from a checkpoint under a new fenced generation."], prevention: ["Use a consensus-backed lease/lock service that returns monotonically increasing fencing tokens.", "Use monotonic time for durations while treating wall-clock time as presentation data.", "Make the storage and external-operation gateways validate generation authority where possible.", "Inject long pauses, delayed renewals, partitions, and clock steps in tests."], tradeoffs: "Fencing requires the protected resource to compare tokens, which may be harder across legacy external systems. Where fencing cannot extend, idempotent generation IDs and reconciliation are necessary; a lease alone is not enough." },
    rubric: [{ dimension: "Failure model", strongSignal: "Explains pause/resume and distinguishes duration clocks from authority.", points: 3 }, { dimension: "Fencing", strongSignal: "Makes the resource reject stale tokens rather than trusting the worker.", points: 3 }, { dimension: "Recovery", strongSignal: "Reconciles duplicate external effects by stable generation IDs.", points: 2 }, { dimension: "Testing", strongSignal: "Includes pauses, partitions, renewal delays, and clock adjustments.", points: 2 }],
    traps: ["Only increasing the lease duration", "Trusting synchronized wall clocks as a lock", "Checking the lease once at job start", "Assuming killing the old process reverses its external effects"],
    related: [{ label: "Time and ordering", href: "/topics/foundations/time-ordering/" }, { label: "Leader election", href: "/topics/consensus/leader-election/" }, { label: "Idempotent workflows", href: "/topics/transactions/idempotent-workflows/" }],
  },
  {
    slug: "stream-backpressure-collapse",
    title: "A slow sink stalls the event pipeline",
    summary: "Consumer lag, oversized batches, and unbounded buffering turn degradation into data loss risk.",
    category: "Messaging", level: "Senior", role: "Data platform owner", duration: 25,
    context: { business: "Fraud decisions depend on events arriving within 30 seconds. A warehouse sink slows during maintenance.", architecture: "Producers → partitioned log → enrichment workers → warehouse and fraud sinks. Workers poll 5,000 events and buffer outputs in memory before acknowledging input.", invariant: "Fraud events must not be lost or reordered per account; warehouse freshness may degrade for two hours." },
    signals: [{ time: "20:00", observation: "Warehouse latency rises 20×; fraud sink remains healthy." }, { time: "20:04", observation: "Worker memory climbs and garbage-collection pauses exceed poll heartbeat intervals." }, { time: "20:07", observation: "Partitions rebalance repeatedly and replay large unacknowledged batches." }, { time: "20:10", observation: "Producer latency increases because broker retention disks approach their alert threshold." }],
    constraints: ["Fraud processing has priority over analytics.", "Events can be replayed for 24 hours if retention remains healthy.", "Dropping warehouse events is not allowed, but delaying them is."],
    prompts: [{ phase: "Diagnose", question: "Trace how one slow sink causes rebalances and upstream pressure.", nudge: "Follow buffer growth, pauses, heartbeats, replay, and retention." }, { phase: "Stabilize", question: "How do you isolate the critical fraud path?", nudge: "Separate queues, concurrency, acknowledgement, and service classes." }, { phase: "Recover", question: "How do you drain backlog without causing another collapse?", nudge: "Control batch size and rate by observed downstream capacity." }, { phase: "Prevent", question: "Where should backpressure be explicit in this topology?", nudge: "Every bounded buffer needs an owner, limit, and overload policy." }],
    response: { diagnosis: "Warehouse output blocks shared workers. Large in-memory batches cause pauses, missed heartbeats, rebalances, and replay amplification. Growing retention then threatens producers and the critical fraud stream.", immediate: ["Isolate or pause the warehouse sink while keeping its offsets and backlog durable in the log.", "Reserve workers, partitions, and broker capacity for fraud traffic.", "Reduce batch and concurrency sizes to bound memory and heartbeat delay.", "Extend retention capacity or reduce noncritical ingress before disks reach emergency limits."], recovery: ["Resume warehouse consumption with adaptive rate limits below measured sink capacity.", "Drain oldest backlog first where ordering permits and monitor queue age, not just count.", "Avoid mass consumer restarts; ramp partitions gradually and verify heartbeat stability.", "Reprocess from committed offsets and validate idempotent warehouse writes."], prevention: ["Use independent consumer groups and resource pools for fraud and warehouse sinks.", "Bound every queue and define pause, spill, shed, or reject behavior.", "Alert on lag age, retention headroom, replay ratio, processing time, and useful throughput.", "Load-test slow-sink and recovery conditions with production-like backlogs."], tradeoffs: "Decoupling sinks spends more compute and may duplicate enrichment work, but it prevents a low-priority analytical dependency from consuming the latency and retention budgets of fraud decisions." },
    rubric: [{ dimension: "Causal chain", strongSignal: "Connects slow sink to memory, pauses, rebalance, replay, and disk pressure.", points: 3 }, { dimension: "Priority", strongSignal: "Isolates fraud resources and defines warehouse degradation explicitly.", points: 3 }, { dimension: "Recovery control", strongSignal: "Drains below capacity with gradual ramp and age-based signals.", points: 2 }, { dimension: "Backpressure", strongSignal: "Assigns limits and overload behavior to every buffer.", points: 2 }],
    traps: ["Adding larger in-memory buffers", "Restarting all consumers simultaneously", "Watching message count but not oldest-event age", "Allowing analytics and fraud to share every resource pool"],
    related: [{ label: "Backpressure", href: "/topics/messaging/backpressure/" }, { label: "Consumer groups", href: "/topics/messaging/consumer-groups/" }, { label: "Load shedding", href: "/topics/reliability/load-shedding/" }],
  },
];
